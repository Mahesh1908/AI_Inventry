# Copy to .env and fill in — never commit the real .env file.
# Discrete vars (preferred) — used to build the mssql connection config.
DB_SERVER=172.16.1.173
DB_PORT=54321
DB_NAME=IDBTesting
DB_USER=Testuser
DB_PASSWORD=123$Testuser
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

PORT=4000
